<phone:PhoneApplicationPage x:Class="FlatBeats.PlayPage"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:ViewModels="clr-namespace:FlatBeats.ViewModels"
                            mc:Ignorable="d"
                            d:DesignWidth="480"
                            d:DesignHeight="768"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            shell:SystemTray.IsVisible="True">
    <phone:PhoneApplicationPage.Resources>
        <ViewModels:PlayPageViewModel x:Key="ViewModel" />
    </phone:PhoneApplicationPage.Resources>
    <phone:PhoneApplicationPage.DataContext>
        <Binding Source="{StaticResource ViewModel}" />
    </phone:PhoneApplicationPage.DataContext>
    <shell:SystemTray.ProgressIndicator>
        <shell:ProgressIndicator IsVisible="{Binding IsInProgress}" IsIndeterminate="True" />
    </shell:SystemTray.ProgressIndicator>
    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn" />
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn" />
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut" />
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut" />
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>
    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <ImageBrush Opacity="0.3" Stretch="UniformToFill">
                <ImageBrush.ImageSource>
                    <BitmapImage UriSource="{Binding Mix.ImageUrl}"
                                 CreateOptions="BackgroundCreation" />
                </ImageBrush.ImageSource>
            </ImageBrush>
        </Grid.Background>
        <controls:Pivot Title="{Binding Title}">
            <controls:PivotItem Header="mix">
                <Grid>
                	<Grid.RowDefinitions>
                		<RowDefinition Height="Auto" MinHeight="183"/>
                		<RowDefinition/>
                	</Grid.RowDefinitions>
                	<Grid.ColumnDefinitions>
                		<ColumnDefinition Width="Auto" MinWidth="197"/>
                		<ColumnDefinition/>
                	</Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Message}"
                               Style="{StaticResource PhoneTextLargeStyle}" Grid.ColumnSpan="2" Grid.RowSpan="2" />
                    <Image Margin="12,0,12,10" d:LayoutOverrides="HorizontalMargin" Width="173">
                    	<Image.Source>
                    		<BitmapImage UriSource="{Binding Mix.ThumbnailUrl}"
                    			CreateOptions="BackgroundCreation" />
                    	</Image.Source>
                    </Image>
                    <TextBlock Text="{Binding Mix.Description}"
                    	Style="{StaticResource PhoneTextLargeStyle}" TextWrapping="Wrap" Margin="12,0" VerticalAlignment="Top" Grid.Column="1" />
                    <StackPanel Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                    <ListBox ItemsSource="{Binding Comments}" Grid.ColumnSpan="2" Grid.Row="1">
                    	<ListBox.ItemsPanel>
                    		<ItemsPanelTemplate>
                    			<VirtualizingStackPanel />
                    		</ItemsPanelTemplate>
                    	</ListBox.ItemsPanel>
                    </ListBox>
                </Grid>
            </controls:PivotItem>
            <controls:PivotItem Header="reviews">
                <Grid>
                    <ListBox ItemsSource="{Binding Comments}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True"
                              IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="appbar_playpause"
                                            Text="play"
                                            IconUri="/icons/appbar.transport.play.rest.png" />
            <shell:ApplicationBarIconButton x:Name="appbar_next"
                                            Text="next"
                                            IconUri="/icons/appbar.transport.ff.rest.png" />
            <shell:ApplicationBarIconButton x:Name="appbar_likeunlike"
                                            Text="like"
                                            IconUri="/icons/appbar.check.rest.png" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>